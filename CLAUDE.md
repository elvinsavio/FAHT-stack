# Project Notes

## Dev Server
The Tailwind CSS watcher runs automatically with the dev server. Do NOT run `npx @tailwindcss/cli` or any Tailwind build commands — output.css is rebuilt automatically on save.
